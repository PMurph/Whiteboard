
define([
    'marionette'
], function (
     Marionette
) {
    'use strict';

    var RoomInvitesBehavior = Marionette.Behavior.extend({
        initialize: function() {
        },
        events: {
        }

    });

    return RoomInvitesBehavior;
});
