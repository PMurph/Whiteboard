{
    "name": "whiteboard",
    "version": "0.0.1",
    "scripts": {
        "preinstall": "npm install -g grunt-cli bower"
    },

    "devDependencies": {
        "grunt": "~0.4.1",
        "grunt-contrib-jshint": "^0.10.0",
        "grunt-contrib-jasmine": "~0.4.2"
    },

    "engines": {
        "node": ">=0.8.0"
    }
}
